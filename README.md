# The CORE Renderer
Renderer with its own mathematics library.

Current phase: alpha

About .crs:
.crs is a file that takes a given .obj file and mutates it to an
unconvertible state so that CORE can read and load it faster with
its last saved position and scale applied.

Passion project, made and maintained by me
